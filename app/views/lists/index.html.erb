<div class="container mt-3">
  <h1> 🎬 All your Movie Lists 🎬 </h1>
  <p> You work so hard, you deserve an escape. 🍿🍷 </p>
  <ol>
    <% @lists.each do |list| %>
      <li><%= link_to list.name, list_path(list)%></li>
    <% end %>
  </ol>
   <%= link_to "Add a new list", new_list_path, style: 'color: #B7B1F2', class: 'btn btn-warning'%>
</div>
